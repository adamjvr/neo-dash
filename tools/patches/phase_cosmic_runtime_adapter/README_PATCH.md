# NeoDash COSMIC runtime adapter

Apply after the runtime event-stream phase and the GTK runtime-adapter phase.

```bash
unzip -o ~/Downloads/neodash_phase_cosmic_runtime_adapter.zip -d .
bash tools/patches/phase_cosmic_runtime_adapter/apply.sh
```

The script resolves the Git repository root from its own location and is safe to
run more than once.
