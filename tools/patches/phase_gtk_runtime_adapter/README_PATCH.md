# NeoDash GTK runtime-adapter phase

Apply from the repository root:

```bash
unzip -o ~/Downloads/neodash_phase_gtk_runtime_adapter.zip -d .
bash tools/patches/phase_gtk_runtime_adapter/apply.sh
```

This phase requires the daemon-owned runtime event-stream phase. The apply script
is path-independent and safe to run more than once.
